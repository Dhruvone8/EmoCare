# ============================================================
# EmoCare — Python Dependencies
# Python 3.13 | venv-based | JupyterLab 4.x
# Install via: pip install -r requirements.txt
# ============================================================

# ── Core & Data Processing ───────────────────────────────────
numpy>=1.26.0
pandas>=2.1.0
scipy>=1.11.0
pyarrow>=14.0.0          # Arrow/Parquet IO, HuggingFace datasets interop

# ── Machine Learning ─────────────────────────────────────────
scikit-learn>=1.3.0
xgboost>=2.0.0
imbalanced-learn>=0.11.0 # SMOTE / class-imbalance handling

# ── Deep Learning & NLP ──────────────────────────────────────
torch>=2.1.0             # CPU build fine for EDA/preprocessing; CUDA optional for training
transformers>=4.35.0     # MentalBERT / BERT / wav2vec2 / HuBERT
datasets>=2.16.0         # HuggingFace datasets — required for GoEmotions Arrow format
tokenizers>=0.15.0

# ── NLP Utilities ────────────────────────────────────────────
nltk>=3.8.1              # Tokenization, stopwords, linguistic markers
spacy>=3.7.0             # Dependency parsing, POS tagging
wordcloud>=1.9.0         # EDA visualizations

# ── Speech / Audio Processing ────────────────────────────────
librosa>=0.10.1          # Audio loading, MFCCs, pitch, prosodic features
soundfile>=0.12.1        # Audio file I/O (WAV)

# ── R Data Format ────────────────────────────────────────────
pyreadr>=0.5.0           # Read StudentLife .Rds (R binary) files in Python

# ── Explainability ───────────────────────────────────────────
shap>=0.44.0             # SHAP for RF/XGBoost
lime>=0.2.0.1            # LIME for text model

# ── Visualization ────────────────────────────────────────────
matplotlib==3.10.3           # pinned: 3.9.x has no cp313-win64 wheel; prevents source-build fallback
seaborn>=0.13.0
plotly>=5.18.0

# ── API & Dashboard ──────────────────────────────────────────
fastapi>=0.104.0
uvicorn>=0.24.0
streamlit>=1.28.0
requests>=2.31.0
pydantic>=2.5.0

# ── Database ─────────────────────────────────────────────────
psycopg2-binary>=2.9.9   # PostgreSQL driver

# ── Jupyter ──────────────────────────────────────────────────
ipykernel>=6.29.0        # Register venv as Jupyter kernel
ipywidgets>=8.1.0        # Interactive widgets in notebooks
jupyter>=1.0.0

# ── Utilities & Config ───────────────────────────────────────
pyyaml>=6.0.1
python-dotenv>=1.0.0
tqdm>=4.66.0

# ── Testing ──────────────────────────────────────────────────
pytest>=7.4.0
