/**
 * EmoCare — Main Page (Placeholder)
 *
 * Replace this file with your full UI.
 * The API client is ready to use:
 *
 *   import { predictText, predictMultimodal, healthCheck } from "@/lib/api";
 *
 * All TypeScript types are in @/types/api
 */
export default function Home() {
  return (
    <main>
      <h1>EmoCare</h1>
      <p>
        Emotion-Aware AI for Early Detection &amp; Support of Mental Health
        Distress
      </p>
      <p>Replace this page with your custom UI components.</p>
    </main>
  );
}
